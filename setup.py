from setuptools import setup

setup (
    name = "rosa",
    python_requires = '>=3.7.2', # I don't actually know what versions I'm compatible with. This is just a paceholder while I figure this out, and to see if it shows up under GitHub dependencies.
)